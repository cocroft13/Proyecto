<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fernando.courthire"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />
    
    <permission
        android:name="android.permission.USER"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="normal" >
    </permission>
    
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/balon_icono"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="fernando.courthire.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="fernando.courthire.PantallaNuevoUsuario"
            android:label="@string/title_activity_pantalla_nuevo_usuario" >
        </activity>
        <activity
            android:name="fernando.courthire.PantallaPistas"
            android:label="@string/title_activity_pantalla_pistas" >
        </activity>
    </application>

</manifest>
